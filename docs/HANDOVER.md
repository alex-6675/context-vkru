# HANDOVER · записка проектировщику (новый диалог) · context-vkru

ЧИТАТЬ В ПОРЯДКЕ: этот файл → docs/INSTRUCTION.md → AGENTS.md →
docs/architecture.md (контракты) → docs/tasks/ (очередь).
Не пересказывать историю заново; отвечать коротко; пользователь устал.

## 0. Роли
- Пользователь: ПЕРВАЯ работа в IT; любит регламенты; устаёт;
  платёжных возможностей нет — платное не предлагать.
  Умеет: commit/push (remote origin2), PowerShell базово, F12+фильтр CTX,
  скриншоты. Не умеет: читать код, раскладку файлов (метафора «оглавление»).
  Чутьё ловит дефекты — доверять. Давать 1–2 действия за ход,
  Кодеру — шаблоны дословно.
- Проектировщик (ты): решения, задания, raw-верификация на GitHub.
- Кодер: coder.qwen.ai (веб) + SourceCraft в VS Code (локально, CDP 9222).
  Исполняет из docs/tasks/; файлы — код-блоками; сам НЕ коммитит;
  «каматоз» = обрыв ответа на «приступаю» → Дополнение Г, дробление,
  «продолжи с последнего артефакта».

## 1. Продукт («вторая память», «тихий инструмент маркетолога»)
- Рыбалка = пространства VK (сейчас) / OK / DZEN (потом): удочка ловит
  персон и сообщества; достойные — в картотеку долгосрочного хранения
  (Базу ТЕЛ).
- ТЕЛО = смартфон; SIM = персона; у каждой персоны СВОЯ шкала групп
  (пресеты, редактируемые).
- Роли доступа: ВЛАДЕЛЕЦ — видит всех; ПЕРСОНАЛ (нанят) — видит
  активность (чем занимался, кому писал, кого отмечал, метки календаря,
  реакции), «запрет общаться» подгружается, белый список закрыт;
  АДМИН (один из акков ТЕЛ) — своя зона ответственности, запрет общения с VIP.
- Метки: ▲ персона/сообщество; ◆ ОТМЕНЁН (Решение №5); «грязь» =
  блеклость + запрет общения; тип метки включает «УЖЕ имеет сопряжение —
  с чем (указать)»: телефон/MPE, Google, другой портал (R8).
- Гугл не дублируем НИКОГДА: обмен vCard 3.0 (CATEGORIES/NOTE/URL,
  X-VK-*) + Google-CSV (Group Membership); календарь — ics позже (R7);
  заметки — наши. MPE (MyPhoneExplorer) = референс интерфейса и форматов.

## 2. Архитектурные решения (не пересматривать без пользователя)
№1 href = первичный ключ; vkit-* запрещены; data-testid — структурные якоря.
№2 Переворот: массового сканирования НЕТ; изъятие в момент ПКМ —
   браузер сам отдаёт linkUrl/pageUrl.
№3 reply= — комментарий; ПКМ по дате = сохранить АВТОРА комментария
   (comment-owner из DOM), пермалинк = место встречи (history).
№4 Одна сущность — несколько форм ссылки (idNNN ↔ короткий):
   каноникализация через редирект + ручное «прикрепить к карточке» (v_08r).
№5 COMMENT — не самостоятельная карточка; ◆ нет.
№6 Персоны (SIM); автопереключение по текущему пользователю VK +
   ручной переключатель.
Контракт карточки: v2 → v3 (persona, groups, links[сопряжения],
access-роли, history, color, visual.faded).

## 3. Процесс
- Репозиторий = офис; чат = обсуждение. Ветка main-qwen_v_03;
  репо github.com/alex-6675/context-vkru; remote origin2.
- docs/tasks/TASK-NNNN.md: NEW→DONE; DONE только после хэша пользователя (Б5).
- Коммиты: тип(область): текст [TASK-NNNN]; области не смешивать;
  пуш пакетом = норма; координата ветки — в задаче (§3.7 INSTRUCTION).
- РЕПО-ГЕЙТ: проектировщик верифицирует raw ДО теста пользователя.
- Порче-невозможность: «_» в коде только через String.fromCharCode(95);
  файл = одна копия; «修改后» = стоп-маркер.
- AGENTS.md: А (целостность/гейты), Б (одна копия), В (локальный агент/CDP),
  Г (дисциплина ответа). INSTRUCTION.md — памятка пользователя.
- PASS/FAIL — ТОЛЬКО пользователь.

## 4. Точка, в которой стоим
Готово: v_01/v_02 PASS; v03r PASS (90d704e); v04r PASS (9c104be);
v05r PASS (2e01d91); v06r PASS (dce4ca7); AGENTS А/Б (a4b6ec9),
В (335f21a), Г (818f807); INSTRUCTION (160c6b7, ef870bc);
дашборд-ремонт (b312b65); чистка reports (e29fd0c).
v07r = FAIL: дефекты D1–D4 (legacy-ромбы на всех датах; ▲-наводнение
на каждое вхождение + накопление при ререндере; цвет не живой;
много окон диалога). Фикс v07f (manifest 0.0.11) ГОТОВ к передаче
(текст — в закрытом диалоге, TASK-0011): автор из комментария, без ◆,
«Удалить карточку», одно окно, unwrap-перерендер, живой onChanged.
d7f2199 = починка инжекции normalize.js (до того метки не работали:
CTX_NORMALIZE is not defined).
База: 38 карточек; тестовый мусор (c13, c23–c30 и т.п. wall-карточки)
удалить кнопкой «Удалить карточку» после v07f.
Очередь: v07f → TASK-0012 v_08r (R1/R4 слияние) → TASK-0013 v_09r
раздвоение ветвей — по docs/SPLIT.md v4; точка — после v_08r
(персоны+группы+экспорт/импорт+сопряжения R8) → v_10r (R7 ics,
косметика: размер окна, иконки, VK-hover у метки, TASK-0004 Gmail-редизайн).
Служебные NEW: TASK-0002 (инфра CDP RunEdgeCdp), TASK-0005 (.vscode),
TASK-0008 (гигиена docs/tasks).

## 5. Памятка себе (новый диалог)
Ресурс беречь: историю не пересказывать; опираться на репо;
ответы короткие; задания — готовыми блоками «передать дословно»;
после каждого вердикта — точка сохранения.
