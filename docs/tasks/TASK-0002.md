# TASK-0002 — chore(infra): RunEdgeCdp.ps1, .gitignore, tasks/launch, opencode.jsonc

**КОММИТ:** `chore(infra): RunEdgeCdp.ps1, .gitignore, tasks/launch, opencode.jsonc [TASK-0002]`
**СТАТУС:** ОЧЕРЕДЬ
**ОБЛАСТЬ:** `infra` — инфраструктура разработки и автоматизации
**ЗАВИСИМОСТЬ:** TASK-0001 = PASS

## ЦЕЛЬ

Инфраструктура для воспроизводимого запуска и отладки: запуск Edge с включённым
DevTools Protocol (CDP) для автоматизированных проверок, игнорирование артефактов,
задачи запуска и конфиг инструмента opencode.

## ИЗМЕНЯЕМЫЕ ФАЙЛЫ

- `scripts/RunEdgeCdp.ps1` — **новый**: запуск Edge с `--remote-debugging-port`
  и отдельным профилем, чтобы CDP-клиент мог подключаться и прогонять проверки
- `.gitignore` — **новый**: `node_modules/`, `dist/`, артефакты Edge-профиля,
  временные файлы ОС — не коммитить
- `tasks/launch` — задачи запуска (запуск dev-сервера дашборда, запуск Edge CDP)
- `opencode.jsonc` — **новый**: конфигурация инструмента opencode для проекта

## ЧТО СДЕЛАНО (что должно быть)

- Ни один из файлов не попадает в пакет `EdgeExtension/` (§2.2 — пакет чистый).
- CDP-профиль изолирован от основного профиля пользователя.
- Артефакты сборки и профили не загрязняют git (покрытие `.gitignore`).

## КОД

Будет приложен проектировщиком в момент выдачи.

## ТЕСТ

1. `pwsh scripts/RunEdgeCdp.ps1` поднимает Edge, порт CDP отвечает.
2. `git status` не показывает `node_modules/`, `dist/`, файлы профиля.
3. Задачи из `tasks/launch` выполняются; `opencode.jsonc` валиден.

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS)

Edge поднимается с CDP, артефакты игнорируются, запуск воспроизводим.

СТОП.
