# TASK-0010 — feat(ext): v06r — маркировка ▲/◆ после F5 + фикс дедупа D1

**КОММИТ:** `feat: разнос v06r (TASK-0010) — маркировка ▲/◆ после F5 и фикс дедупа D1 (manifest 0.0.9)`
**СТАТУС:** IN PROGRESS · разнос выполнен (хэш получен), ручной тест ожидает Пользователя (§5.6)
**ХЭШИ:** `09498688` (feat: разнос v06r) · хэш docs/report — уточнить после коммита
**РУЧНОЙ ТЕСТ:** report_2026-08-21_01.md — ожидает проверки Пользователя в Edge (§5.6)
**ОБЛАСТЬ:** `ext` — EdgeExtension · **МАНИФЕСТ:** 0.0.9
**ОСНОВАНИЕ:** текст проектировщика · дамп `task-0010.log` · Фикс D1 (дедуп COMMENT)
**ЗАВИСИМОСТЬ:** TASK-0009 = PASS (ручной тест) → v06r на его основе

## ЦЕЛЬ

Маркировка сохранённых ссылок после F5: ▲ (персонаж/сообщество) и ◆ (закладка
комментария); фикс дедупа D1 — ключ COMMENT по `portal+id+replyId`, чтобы разные
комментарии одного поста не схлопывались в одну карточку.

## ФАЙЛЫ

- `src/core/messaging.js`: `CTX_BUILD = "v06r"`, `CTX_MSG.CAPTURED`.
- `src/core/normalize.js`: добавлен `replyOf(url)` — извлечение `reply=` из URL.
- `src/core/storage.js`: **БЕЗ ИЗМЕНЕНИЙ** (как v05r).
- `src/background.js`: фикс D1 — дедуп COMMENT по `portal+id+replyId`;
  `replyId` сохраняется в `identity.replyId`; `nextCardId` по `cardId`; после
  сохранения шлёт `CTX_MSG.CAPTURED` в content.
- `src/content.js`: при старте строит индекс `byId` / `byComment(id#reply)` /
  `legacy`; маркирует ссылки: ▲ (персонаж/сообщество), ◆ (комментарий);
  только добавленный `span.ctx-mark`, атрибуты VK не трогаются (§H);
  WeakSet против дублей; MutationObserver 600 мс.
- `src/styles.css` (**НОВЫЙ**): `.ctx-mark` (#2b6fb3), `.ctx-mark-c` (#8a6d1f).
- `manifest.json`: `version 0.0.9`, `permissions ["contextMenus","storage"]`,
  в `content_scripts` добавлены `src/core/normalize.js` и `css: ["src/styles.css"]`.

## ТЕСТ (пользователь)

1. Reload расширения → версия 0.0.9.
2. Сохранить персонажа/сообщество → после F5 на ссылке метка ▲.
3. Сохранить закладку комментария (`reply=`) → после F5 метка ◆.
4. D1: два разных комментария одного поста = две карточки (не схлопываются).
5. Вёрстка VK не повреждена (меняется только добавленный span).

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS)

Метки ▲/◆ появляются на сохранённых ссылках после F5; разные комментарии одного
поста — отдельные карточки; вёрстка цела. Карточка расширения без ошибок (0.0.9).
→ report_2026-08-21_01.md RESULT = PASS, хэш Пользователя.

Б5: статус DONE — только после хэша коммита Пользователя.

СТОП.