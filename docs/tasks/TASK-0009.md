# TASK-0009 — feat(ext): v05r — база, карточка v2, дедуп

**КОММИТ:** `feat(ext): v05r — база, карточка v2, дедуп [TASK-0009]`
**СТАТУС:** DONE · хэш получен (Б5)
**ХЭШИ:** `2e01d91` (feat(ext): v05r) · `7129c46` (docs: TASK-0009.md + report_2026-08-21.md)
**РУЧНОЙ ТЕСТ:** RESULT_v05r.md — ожидает проверки Пользователя в Edge (§5.6)
**ОБЛАСТЬ:** `ext` — EdgeExtension · **МАНИФЕСТ:** 0.0.8
**ОСНОВАНИЕ:** текст проектировщика · Дополнение №3 (контракт карточки v2) · Решение №3 (правило типов)
**ЗАВИСИМОСТЬ:** TASK-0006 = PASS (9c104be)

## ЦЕЛЬ

Сохранение записи в `chrome.storage.local` по контракту карточки v2; дедуп по
`portal+id`; правило типов (Решение №3: ссылка с `reply=` → kind COMMENT);
персистентность через F5 и перезапуск Edge.

## ФАЙЛЫ

- `src/core/storage.js` (**НОВЫЙ**): ключ `"ctxdb"`, `DB = {version:1, cards:[]}`,
  `loadDb` / `saveDb` через `chrome.storage.local`.
- `src/core/normalize.js`: короткие id **БЕЗ** ведущего слэша; `metPostOf`:
  значение `w=` матчить regex **без** `"^/"` (через U, по А1).
- `src/core/messaging.js`: `CTX_BUILD = "v05r"`.
- `src/background.js`: после normalize — запись:
  - url содержит `"reply="` → kind COMMENT;
  - дедуп: карточка с тем же `portal+id` в `identities` → не создавать,
    обновить `lastSeen` + `history`, лог `"уже в базе (card cN)"`;
  - иначе новая карточка по контракту v2: `cardId`, `created`, `displayName ""`,
    `note ""`, `status "saved"`, `visual {faded:false}`,
    `identities [{portal,id,url,name:"",metAt,metUrl}]`,
    `access {ownerOnly:false, staffContact:"allowed"}`,
    `history [{date,action:"captured",portal,url}]`;
    лог `"saved card cN (total N)"`.
- `src/content.js`: при старте читать db, лог `"[CTX v05r] db: N cards"` + список id.
- `manifest.json`: `version 0.0.8`, `permissions ["contextMenus","storage"]`,
  `storage.js` подключён в `content_scripts` **до** `content.js`.

## ТЕСТ (пользователь)

1. ПКМ на имени → «Сохранить персонажа» → SW: `"saved card c1 (total 1)"`.
2. Тот же человек, та же форма ссылки → `"уже в базе (card c1)"`.
3. ПКМ по дате комментария → `kind COMMENT` в логе.
4. F5 → страница: `"db: N cards: …"` с тем же N; перезапуск Edge → то же N.

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS)

Запись создаётся по контракту v2, дедуп по `portal+id` работает, kind COMMENT
определяется по `reply=`, база переживает F5 и перезапуск. Карточка расширения
без ошибок (0.0.8). → RESULT_v05r.md, хэш Пользователя.

Б5: статус DONE — только после хэша коммита Пользователя.

СТОП.
