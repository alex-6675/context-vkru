# TASK-0011 — fix(ext): v07f — меню коррекции карточки, цвет ника, блеклость (фикс-итерация)

**КОММИТ:** `fix(ext): v07f — меню коррекции карточки, цвет ника, блеклость [TASK-0011]`
**СТАТУС:** NEW (фикс-итерация после FAIL)
**ХЭШ:** <заполняет Пользователь после пуша>
**ОБЛАСТЬ:** `ext` — EdgeExtension · **МАНИФЕСТ:** 0.0.11
**ОСНОВАНИЕ:** сводка проектировщика (v06r → PASS, dce4ca7) · Дополнение №3 (контракт карточки v2) · R3 (заливка ника цветом картотеки) · вердикт v07r = FAIL (коммит 2561ea8)
**ЗАВИСИМОСТЬ:** TASK-0010 = PASS (dce4ca7)

## ВЕРДИКТ ПО ПРЕДЫДУЩЕЙ СБОРКЕ v07r

- **v07r = FAIL** (зафиксировано в диалоге `reports/Coder_26-8-22.md`, коммит `2561ea8`).
- По §5.1: при FAIL остаёмся внутри этапа — фикс-итерация **v07f**, версия манифеста **0.0.11**.
- Конкретные пункты, не прошедшие ручной тест, — в `reports/Coder_26-8-22.md`
  (содержимое диалога на момент слияния было недоступно исполнителю — при
  расхождении приоритет за диалогом; см. ОТЧЁТ ниже).

## ЦЕЛЬ (без изменений — критерии PASS)

Клик по метке открывает карточку коррекции; маркировка рисует ▲ + заливку ника
цветом карточки (~80%); «грязь» — блеклость; перерисовка без перезагрузки при
изменении базы.

## ФАЙЛЫ (состав v07r — правятся точечно по результатам теста)

- `EdgeExtension/dialog.html` + `src/dialog.js`: окно карточки
  (chrome.windows.create из SW, url `dialog.html#cardId`); поля: displayName,
  note, status (saved/dirt/partner/vip), цвет (палитра 5), identities — список
  только для чтения; «Сохранить» → saveDb → закрыть. Скрипты: storage.js + dialog.js.
- `src/background.js`: приём OPEN_CARD от контента → windows.create.
- `src/content.js`: клик по span.ctx-mark → OPEN_CARD; рендер: обёртка
  span.ctx-hl вокруг якоря (background=цвет карточки, opacity .8) + ▲;
  status dirt → класс ctx-faded (opacity .45 + grayscale);
  chrome.storage.onChanged → пересобрать индекс и перерендерить
  (снять свои обёртки, применить заново).
- `src/styles.css`: .ctx-hl, .ctx-faded; только классы ctx-*.
- `src/core/messaging.js`: `CTX_BUILD = "v07f"` + тип `OPEN_CARD`.
- `manifest.json`: version 0.0.11; новых permissions НЕ нужно.

## ШАГИ (по Дополнению Г — один ответ = один шаг)

1. Разобрать пункты FAIL из `reports/Coder_26-8-22.md` (или из сообщения Пользователя).
2. Точечные правки по каждому пункту (без переписывания целых файлов, если не сказано «заменить целиком»).
3. manifest 0.0.11 + `CTX_BUILD = "v07f"` + отчёт.

## ТЕСТ (пользователь) — критерии PASS, те же пункты 1–5

1. Клик по ▲ → окно карточки.
2. Статус «грязь» → ник блеклый без F5.
3. Цвет → заливка ника ~80%.
4. Примечание и статус живут после F5.
5. Клики VK не нарушены.

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS)

Пункты 1–5 + устранение конкретных замечаний из `reports/Coder_26-8-22.md`.
R1/R2 в этот этап НЕ входят (пойдут в TASK-0012 / v08r).

Б5: статус DONE — только после хэша коммита Пользователя.

СТОП.
