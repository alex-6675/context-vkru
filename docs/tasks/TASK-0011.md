# TASK-0011 — feat(ext): v07g — стекло, скальпель при нажатии, индикатор

> v07g ФИНАЛЬНЫЙ (заменяет все прежние тексты v07g).
> Диагноз v07d: marked 11 / wrappers in DOM 0 — React VK стирает наши узлы.
> Инъекция в чужой DOM ОСУЖДЕНА; отрисовка — на собственном стекле;
> скальпель — через доступ браузера «при нажатии».

**КОММИТ:** `feat(ext): v07g — стекло, скальпель при нажатии, индикатор [TASK-0011]`
**СТАТУС:** NEW
**ХЭШ:** <заполняет Пользователь после пуша>
**ОБЛАСТЬ:** `ext` — EdgeExtension · **МАНИФЕСТ:** 0.0.18 · **CTX_BUILD:** "v07g"
**ОСНОВАНИЕ:** диагноз v07d (React стирает узлы)
**ЗАВИСИМОСТЬ:** v07d

## ПРАВКИ

1. **`src/ui/layer.js` (НОВЫЙ) = СТЕКЛО:**
   - `div#ctx-glass` на `document.body`; CSS: `position:fixed; inset:0;
     pointer-events:none; z-index:2147483000; overflow:hidden;`
     дети `.ctx-g-hl`/`.ctx-g-mark` — `position:absolute;`.
   - `draw(scope)`: `glass.replaceChildren()`; по сохранённым текстовым якорям
     (href → normalize → INDEX.byId; если задан scope — только внутри него):
     `r=getBoundingClientRect()`; `r.width==0` → пропуск; `.ctx-g-hl` поверх ника
     (цвет карточки ~25%, dirt → grayscale+opacity); `.ctx-g-mark` «▲» у `r.right+2`
     (`pointer-events:auto`; title «CTX: cN · имя»; click → OPEN_CARD).
   - Триггеры: draw при старте, SPA-навигация, observer (600 мс), resize,
     scroll (rAF), интервал 2 с, `storage.onChanged`, CTX_SYNC. Лог `glass: N markers`.
   - Экспорт `CTX_LAYER {init, rescan, draw}`.

2. **СКАЛЬПЕЛЬ ЧЕРЕЗ БРАУЗЕР:** рекомендуемый доступ к сайту —
   «Разрешить только при нажатии» (пользователь переключает один раз; в
   манифесте этого нет). Нет доступа = контент не запущен = «стекла нет», тишина.
   **CTX_TOGGLE УБРАТЬ.**

3. **ИНДИКАТОР — badge на значке, три состояния** (контент шлёт в SW,
   `chrome.action.setBadgeText/setBadgeBackgroundColor`):
   - нет бейджа (серый) = стекла нет;
   - жёлтый «…» = включено, идёт поиск координат (draw в процессе);
   - зелёный «N» = стекло успешно внедрено (N = число маркеров).

4. **КЛИК ПО ЗНАЧКУ** (`action.onClicked` → CTX_SYNC во вкладку):
   - нет выделения на странице → полная перерисовка стекла
     («искать новые координаты»; лечит «стекло разбито»);
   - есть текстовое выделение → ЛОКАЛЬНЫЙ СКАЛЬПЕЛЬ: маркеры только внутри
     ближайшего контейнера, содержащего выделение (операция с секцией страницы).

5. **`src/content.js`** = тонкая точка входа: старт, лог db, CAPTURED,
   SAVE_AUTHOR, NAME_HINT (guard: только пустое displayName), MET_HINT,
   приём CTX_SYNC, вызов `CTX_LAYER.init()`. Изъятие работает всегда
   (при данном доступе).

6. **В ШРЕДЕР:** wrap/unwrap/span.ctx-hl/containerOf/perCard и стили
   .ctx-hl/.ctx-mark — НЕ переносить, удалить.

7. **`src/dialog.js` → `src/ui/dialog.js`**; в dialog.html поправить script src.
   Поведение диалога не менять (окна закрываются, логи saved/удалена,
   тип персона/сообщество, точка встречи, ответ).

8. **manifest:** js-порядок = messaging, normalize, storage, ui/layer.js,
   content; version → 0.0.18.

9. **architecture.md:** §H — «собственный слой = оверлей-стекло по координатам;
   в DOM портала ничего; скальпель = доступ при нажатии; индикатор трёх
   состояний на значке»; §L — к факту (ui/layer.js, ui/dialog.js;
   adapters снят Решение №2; popup.js = R13).

## ТЕСТ (пользователь)

1. Доступ «при нажатии» НЕ дан: на vk.ru стекла нет, бейдж серый, тишина.
2. Дать доступ, кликнуть значок → жёлтый «…» → зелёный «N», ▲ у сохранённых,
   подложка ~25%, лог `glass: N markers`.
3. Выделить текст на странице → клик по значку → маркеры только внутри
   контейнера с выделением (локальный скальпель).
4. Клик без выделения → полная перерисовка (лечит «разбитое» стекло).
5. ПКМ-изъятие работает всегда; клик по ▲ → карточка.
6. F5 → стекло вернулось (access + init при старте).

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS)

Пункты 1–6; React не стирает стекло (оно вне его DOM — структурно).

Б5: статус DONE — только после хэша коммита Пользователя.

СТОП.
