# TASK-0011 — feat(ext): v07g — стекло, скальпель, структура src/ui

> v07g единый. Диагноз v07d: marked 11 / wrappers in DOM 0 — React VK стирает
> наши узлы. Инъекция в чужой DOM ОСУЖДАЕТСЯ (не переносится!); отрисовка — на
> собственном стекле; включение — скальпель.

**КОММИТ:** `feat(ext): v07g — стекло, скальпель, структура src/ui [TASK-0011]`
**СТАТУС:** NEW
**ХЭШ:** <заполняет Пользователь после пуша>
**ОБЛАСТЬ:** `ext` — EdgeExtension · **МАНИФЕСТ:** 0.0.18 · **CTX_BUILD:** "v07g"
**ОСНОВАНИЕ:** диагноз v07d (React стирает узлы)
**ЗАВИСИМОСТЬ:** v07d

## ПРАВКИ

1. **`src/ui/layer.js` (НОВЫЙ) = СТЕКЛО:**
   - `div#ctx-glass` на `document.body`; CSS: `position:fixed; inset:0;
     pointer-events:none; z-index:2147483000; overflow:hidden;` дети
     `.ctx-g-hl`/`.ctx-g-mark` — `position:absolute`.
   - `draw()`: `glass.replaceChildren()`; по сохранённому текстовому якорю
     (href → normalize → INDEX.byId): `r=getBoundingClientRect()`;
     `r.width==0` → пропуск; `.ctx-g-hl` поверх ника (цвет карточки ~25%,
     dirt → grayscale+opacity); `.ctx-g-mark` «▲» у `r.right+2`
     (`pointer-events:auto`; title «CTX: cN · имя»; click → OPEN_CARD).
   - СКАЛЬПЕЛЬ: по умолчанию ВЫКЛ (observer не подключен, стекло пусто, тишина);
     ВКЛ/ВЫКЛ — клик по значку (`action.onClicked` → `CTX_TOGGLE`; состояние в
     storage; badge «on»/""; лог «scalpel: ON/OFF»). При ВКЛ триггеры: draw
     сразу, SPA-навигация, observer (только при ВКЛ, 600 мс), resize, scroll
     (rAF), интервал 2 с, storage.onChanged. Лог «glass: N markers».
   - Экспорт `CTX_LAYER {init, rescan}` (+ `toggle` — переключение по CTX_TOGGLE).

2. **`src/content.js` = тонкая точка входа:** старт, лог db, CAPTURED,
   SAVE_AUTHOR, NAME_HINT (guard: только пустое displayName — на стороне SW),
   MET_HINT, приём `CTX_TOGGLE`, вызов `CTX_LAYER.init()`. ИЗЪЯТИЕ работает всегда.

3. **В ШРЕДЕР:** весь код wrap/unwrap/span.ctx-hl/containerOf/perCard и стили
   `.ctx-hl`/`.ctx-mark` — НЕ переносить, удалить.

4. **`src/dialog.js` → `src/ui/dialog.js`;** в `dialog.html` поправить script src
   (+ добавлен `messaging.js` — без него CTX_MSG был undefined и «ясный» лог
   ломал закрытие окна). Поведение диалога не менять (окна закрываются, логи
   saved/удалена, тип персона/сообщество, точка встречи, ответ).

5. **manifest:** js-порядок = messaging, normalize, storage, ui/layer.js, content;
   version → 0.0.18.

6. **architecture.md:** §H — «собственный слой = оверлей-стекло по координатам;
   в DOM портала не добавляется ничего»; §L — к факту: ui/layer.js = стекло,
   ui/dialog.js; adapters/vkru.js «снят (Решение №2)»; popup.js «будущая
   картотека (R13)». (Файл поправки: docs/ARCHITECTURE_V07G_ADDENDUM.md)

## ТЕСТ (пользователь)

1. vk.ru, по умолчанию: меток НЕТ, в консоли нет «observer: scan scheduled» / «glass:».
2. Клик по значку → badge «on», «scalpel: ON», «glass: N markers»; ▲ у сохранённых,
   подложка ~25% цвета карточки; dirt — блеклая.
3. Повторный клик → badge пустой, «scalpel: OFF», стекло очищено.
4. F5 со включённым скальпелем → метки вернулись (состояние из storage).
5. Метки НЕ стираются при подгрузке ленты/ре-рендере React (живые виды).
6. ПКМ-изъятие («Сохранить персонажа/сообщество», автор по дате) работает и при
   ВЫКЛ скальпеле; клик по ▲ → окно карточки; «Сохранить»/«Удалить» закрывают окно.

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS)

Пункты 1–6.

Б5: статус DONE — только после хэша коммита Пользователя.

СТОП.
