# TASK-0011 — fix(ext): v07f6 — метка на каждое вхождение, без предохранителей

> Вердикт: v07f5 = FAIL (предохранители съедают метки) → фикс v07f6.
> Три правки, остальное НЕ трогать.

**КОММИТ:** `fix(ext): v07f6 — метка на каждое вхождение, без предохранителей [TASK-0011]`
**СТАТУС:** NEW
**ХЭШ:** <заполняет Пользователь после пуша>
**ОБЛАСТЬ:** `ext` — EdgeExtension · **МАНИФЕСТ:** 0.0.16 · **CTX_BUILD:** "v07f6"
**ОСНОВАНИЕ:** предохранители «≤3 на страницу» и «одна на контейнер» съедают
метки в плотных видах (мессенджер, треды) — пользователь видит «маркировка исчезла»
**ЗАВИСИМОСТЬ:** v07f5

## ПРАВКИ

1. **МЕТКА НА КАЖДОЕ ВХОЖДЕНИЕ:** в `scan()` ПОЛНОСТЬЮ убрать `perCard` и
   `cardCount` (контейнерную логику и лимит 3). Метить КАЖДЫЙ текстовый якорь
   сохранённой карточки (один ▲ на вхождение).
   Сохранить: unwrap всех `span.ctx-hl` в начале scan; пропуск якорей без
   текста; пропуск якорей внутри `span.ctx-hl`; флаг `selfChange`;
   лог `"marked N anchors"`.

2. **НЕ менять ничего** (окна, логи, тип, живая перекраска — как в v07f5).

3. **NAME_HINT (background.js):** писать `displayName` ТОЛЬКО если он пуст;
   если `displayName` уже задан (пользователем или ранее) — НЕ трогать,
   лог `"имя НЕ тронуто: cN (уже задано)"`. `identity.name` обновлять только
   если пуст. В db писать лишь при фактическом изменении.

## НЕ МЕНЯТЬ

`manifest.json`: ТОЛЬКО version → 0.0.16.

## ТЕСТ (пользователь)

1. Плотный вид (мессенджер / тред) с несколькими вхождениями сохранённой
   карточки → ▲ стоит у КАЖДОГО вхождения (не «исчезает» после третьего).
2. После обновления страницы метки возвращаются у всех вхождений.
3. NAME_HINT: у карточки с уже заданным именем displayName НЕ перезаписывается
   (в SW-консоли «имя НЕ тронуто: cN (уже задано)»).
4. У карточки без имени имя подставляется из первого якоря («имя сохранено»).

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS)

Пункты 1–4.

Б5: статус DONE — только после хэша коммита Пользователя.

---

## ПРЕЖНЯЯ ИТЕРАЦИЯ — v07f5 (заменена, для истории)

v07f5: окна («Сохранить»/«Удалить» закрывают окно после saveDb), единственный
писатель (свежий loadDb, удаление filter — и c25-подобные), «ясные» логи через
LOG в SW, тип персона/сообщество (R16, селект под статусом), живая перекраска
(D21: onChanged → buildIndex → scan, цвет из индекса).

СТОП.
