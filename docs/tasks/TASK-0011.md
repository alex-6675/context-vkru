# TASK-0011 — feat(ext): v07r — меню коррекции карточки, цвет ника, блеклость

**КОММИТ:** `feat(ext): v07r — меню коррекции карточки, цвет ника, блеклость [TASK-0011]`
**СТАТУС:** NEW
**ХЭШ:** <заполняет Пользователь после пуша>
**ОБЛАСТЬ:** `ext` — EdgeExtension · **МАНИФЕСТ:** 0.0.10
**ОСНОВАНИЕ:** сводка проектировщика (v06r → PASS, dce4ca7) · Дополнение №3 (контракт карточки v2) · R3 (заливка ника цветом картотеки)
**ЗАВИСИМОСТЬ:** TASK-0010 = PASS (dce4ca7)

## ЦЕЛЬ

Клик по метке открывает карточку коррекции; маркировка рисует ▲ + заливку ника
цветом карточки (~80%); «грязь» — блеклость; перерисовка без перезагрузки при
изменении базы.

## ФАЙЛЫ

- `EdgeExtension/dialog.html` + `src/dialog.js` (**НОВЫЕ**): окно карточки
  (chrome.windows.create из SW, url `dialog.html#cardId`); поля: displayName,
  note, status (saved/dirt/partner/vip), цвет (палитра 5), identities — список
  только для чтения; «Сохранить» → saveDb → закрыть. Скрипты: storage.js + dialog.js.
- `src/background.js`: приём OPEN_CARD от контента → windows.create.
- `src/content.js`: клик по span.ctx-mark → OPEN_CARD; рендер: обёртка
  span.ctx-hl вокруг якоря (background=цвет карточки, opacity .8) + ▲;
  status dirt → класс ctx-faded (opacity .45 + grayscale);
  chrome.storage.onChanged → пересобрать индекс и перерендерить
  (снять свои обёртки, применить заново).
- `src/styles.css`: .ctx-hl, .ctx-faded, палитра; только классы ctx-*.
- `src/core/messaging.js`: `CTX_BUILD = "v07r"` + тип `OPEN_CARD`.
- `manifest.json`: version 0.0.10; новых permissions НЕ нужно.

## ШАГИ (по Дополнению Г — один ответ = один шаг)

1. dialog.html + dialog.js
2. content.js (рендер + onChanged) + background.js (OPEN_CARD → windows.create) + messaging.js
3. styles.css + manifest (0.0.10) + отчёт

## ТЕСТ (пользователь)

1. Клик по ▲ → окно карточки.
2. Статус «грязь» → ник блеклый без F5.
3. Цвет → заливка ника ~80%.
4. Примечание и статус живут после F5.
5. Клики VK не нарушены.

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS)

Пункты 1–5. R1/R2 в этот этап НЕ входят (пойдут в TASK-0012 / v08r).

Б5: статус DONE — только после хэша коммита Пользователя.

СТОП.
