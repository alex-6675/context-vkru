# TASK-0011 — fix(ext): v07f2 — идемпотентность, единый треугольник, заливка ника, «когда/кто/ответ»

> Исправленный TASK-0011 (v07f2). Заменяет прежние тексты v07f/v07f2 целиком.

**КОММИТ:** `fix(ext): v07f2 — идемпотентность, единый треугольник, заливка ника, «когда/кто/ответ» [TASK-0011]`
**СТАТУС:** NEW
**ХЭШ:** <заполняет Пользователь после пуша>
**ОБЛАСТЬ:** `ext` — EdgeExtension · **МАНИФЕСТ:** 0.0.12 · **CTX_BUILD:** "v07f2"
**ОСНОВАНИЕ:** reports/v_03/RESULT_v07r.md (FAIL) · Решение №7 (визуал)
**ЗАВИСИМОСТЬ:** v07f

Остальное НЕ трогать (одно окно, «Удалить карточку», NAME_HINT, живой onChanged).

## ПРАВКИ

1. **ИДЕМПОТЕНТНОСТЬ:** в начале `scan()` снять ВСЕ `span.ctx-hl` по классу
   (якорь вернуть на место, обёртку удалить); свои DOM-правки под флагом
   `selfChange`; MutationObserver при флаге scan НЕ планирует.

2. **КОНТЕЙНЕРЫ:** `li → [data-testid="post"] → wall_comments_comment_root →
   wall_comments_comment_in_thread → [role="dialog"] → section → body`;
   одна метка на карточку на контейнер; НЕ БОЛЕЕ 3 на карточку на страницу;
   только якоря с текстом; якоря внутри `span.ctx-hl` пропускать.

3. **ВИЗУАЛ (Решение №7):** `.ctx-mark` — ВСЕГДА `color:#8a94a3`
   (вариации и `ctx-mark-c` убрать); `span.ctx-hl` = подсветка букв ника
   цветом карточки (background = card.color ~80%, padding 0 2px, radius 2px);
   status dirt → `.ctx-faded` на обёртке (opacity .45 + grayscale).

4. **УДОСТОВЕРЕНИЕ БЕЗ МУСОРА:** `identity.url = cleanUrl(link)`:
   оставлять ТОЛЬКО reply/thread/w (trackcode и пр. отсекаются).

5. **КАРТОЧКА:** секция «ТОЧКА ВСТРЕЧИ И ИСТОРИЯ» (чтение, последние 5):
   «дата · кто (id/displayName) · точка встречи (url) · ответ»;
   history-запись = `{date, action, url, answer}`;
   ниже селект «ответ последнего контакта»
   (нет ответа / ответила / согласилась / отказ) → пишет `answer`
   в последнюю запись, saveDb.

6. **manifest.json:** ТОЛЬКО version → 0.0.12.

## ТЕСТ (пользователь)

1. ПКМ по дате → «автор сохранён/уже в базе + точка встречи», url без trackcode/recom.
2. ▲ по одному на комментарий (серый, единый), после обновления не прибавляется; ≤3 на карточку.
3. Заливка букв ника — цветом карточки; «грязь» → блеклость.
4. В диалоге — «Точка встречи и история» (последние 5: дата · кто · url · ответ);
   селект «ответ последнего контакта» пишет ответ и сохраняет.
5. Карточка удаляется кнопкой; цвет/dirt живут без F5; одно окно; имя сохраняется.

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS)

Пункты 1–5 теста.

Б5: статус DONE — только после хэша коммита Пользователя.

СТОП.
