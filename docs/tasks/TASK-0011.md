# TASK-0011 — fix(ext): v07f4 — единственный писатель, без воскрешений

> Вердикт: v07f3 = FAIL по D18 (воскрешение/стирание) → фикс v07f4.
> Правило ЕДИНСТВЕННОГО ПИСАТЕЛЯ: каждый saveDb — ТОЛЬКО после свежего loadDb
> в том же обработчике (read-modify-write); никаких «долгоживущих» объектов db.

**КОММИТ:** `fix(ext): v07f4 — единственный писатель, без воскрешений [TASK-0011]`
**СТАТУС:** NEW
**ХЭШ:** <заполняет Пользователь после пуша>
**ОБЛАСТЬ:** `ext` — EdgeExtension · **МАНИФЕСТ:** 0.0.14 · **CTX_BUILD:** "v07f4"
**ОСНОВАНИЕ:** v07f3 = FAIL по D18
**ЗАВИСИМОСТЬ:** v07f3

Остальное НЕ трогать (одно окно, «Удалить карточку», NAME_HINT, живой onChanged,
идемпотентность, контейнеры ≤3, cleanUrl, точка встречи=коммент, заливка ~25%).

## ПРАВКИ

1. **dialog.js:** кнопки «Сохранить», «Удалить карточку», селект ответа — в каждом
   обработчике `const db = await CTX_STORAGE.loadDb();` применить изменение к
   СВЕЖЕЙ db; `await saveDb(db)`; закрыть. db, загруженную при открытии окна,
   для записи НЕ использовать.

2. **background.js:** проверить, что capture / NAME_HINT / точка встречи каждый
   раз читают свежую db (по коду — да, подтверждено).

3. **content.js:** только чтение; если есть saveDb — убрать (отсутствует — только чтение).

4. **Логи:** после каждого saveDb — `[CTX v07f4] db записана (total N)`;
   при удалении — `[CTX v07f4] card cN удалена`.

5. **manifest.json:** ТОЛЬКО version → 0.0.14.

6. **Селект «ответ последнего контакта»:** значения ТОЛЬКО «ожидание / да / нет»;
   при чтении старых записей маппинг: нет ответа→ожидание;
   ответила|согласилась→да; отказ→нет.

## ТЕСТ (пользователь)

1. Сохранить карточку, затем удалить — карточка исчезает из базы и не «воскресает».
2. Открыть две карточки, править обе — изменения не затирают друг друга.
3. После каждой записи в консоли SW — «db записана (total N)»; при удалении — «card cN удалена».
4. Селект ответа: только ожидание/да/нет; старые «ответила/согласилась/отказ» отображаются как да/да/нет.
5. Точка встречи, заливка ~25%, ▲ ≤3, одно окно, живой onChanged — без регрессий.

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS)

Пункты 1–5. Воскрешений/стираний нет.

Б5: статус DONE — только после хэша коммита Пользователя.

СТОП.
