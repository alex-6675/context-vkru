# TASK-0011 — fix(ext): v07f5 — окна, надёжное удаление, логи, тип, живая перекраска

> Вердикт: v07f4 = FAIL по пакету → фикс v07f5. Пять правок, остальное НЕ трогать.

**КОММИТ:** `fix(ext): v07f5 — окна, надёжное удаление, логи, тип, живая перекраска [TASK-0011]`
**СТАТУС:** NEW
**ХЭШ:** <заполняет Пользователь после пуша>
**ОБЛАСТЬ:** `ext` — EdgeExtension · **МАНИФЕСТ:** 0.0.15 · **CTX_BUILD:** "v07f5"
**ОСНОВАНИЕ:** v07f4 = FAIL по пакету
**ЗАВИСИМОСТЬ:** v07f4

## ПРАВКИ

1. **ОКНА:** «Сохранить» и «Удалить карточку» после `saveDb` вызывают
   `window.close()`. Никаких висящих окон.

2. **ЕДИНЫЙ ПИСАТЕЛЬ В ДИАЛОГЕ:** все обработчики записи (сохранить/удалить)
   начинают с `const db = await loadDb();` → mutate → `await saveDb(db)`.
   Удаление: `db.cards = db.cards.filter(c => c.cardId !== id)`.
   Проверить на c25-подобных (старые COMMENT с replyId) — удаляется всегда.

3. **ЛОГИ:** через `sendMessage` в SW: `"[CTX v07f5] card cN saved"`,
   `"[CTX v07f5] card cN удалена"` — сохранение становится «ясным».

4. **ГРАНИЦА ПЕРСОНА/СООБЩЕСТВО (R16):** при изъятии писать
   `card.type = PERSON|COMMUNITY`; в карточке — селект «персона / сообщество»
   под статусом, с явной разделительной линией; правка типа сохраняется в `type`.

5. **ЖИВАЯ ПЕРЕКРАСКА (D21):** `storage.onChanged` → `buildIndex(newValue)` →
   `scan()`; `fillOf` читает цвет ИЗ НОВОГО индекса (не из замыкания).

`manifest.json`: ТОЛЬКО `version → 0.0.15`.

## ТЕСТ (пользователь)

1. «Сохранить»/«Удалить» — окно закрывается, не висит.
2. Удаление c25-подобной (COMMENT с replyId) — удаляется всегда.
3. В SW-консоли — `card cN saved` / `card cN удалена`.
4. Тип «персона/сообщество» в карточке сохраняется; при изъятии пишется верно.
5. Смена цвета/статуса в карточке → перекраска на странице без F5.

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS)

Пункты 1–5.

Б5: статус DONE — только после хэша коммита Пользователя.

СТОП.
