# TASK-0011 — diag(ext): v07d — самопроверка маркировки

> Диагностика v07d. Поведение НЕ менять — только логи-самопроверки.

**КОММИТ:** `diag(ext): v07d — самопроверка маркировки [TASK-0011]`
**СТАТУС:** NEW
**ХЭШ:** <заполняет Пользователь после пуша>
**ОБЛАСТЬ:** `ext` — EdgeExtension · **МАНИФЕСТ:** 0.0.17 · **CTX_BUILD:** "v07d"
**ОСНОВАНИЕ:** нужна инструментальная проверка, что метки реально ставятся в DOM
**ЗАВИСИМОСТЬ:** v07f6

## ПРАВКИ (только логи, поведение не меняется)

1. В `scan()` после разметки: `setTimeout 500 мс` →
   `[CTX v07d] marked in scan: M; wrappers in DOM: N`
   (`N = document.querySelectorAll('.ctx-hl').length`).

2. Контент хранит `LAST_ID` из последнего CAPTURED; в том же таймауте:
   `[CTX v07d] anchors for <LAST_ID>: K; wrapped: W`, где
   `K` — число текстовых якорей с этим id,
   `W` — сколько из них внутри `.ctx-hl`.

3. В MutationObserver при планировании: `[CTX v07d] observer: scan scheduled`.

4. `manifest.json`: ТОЛЬКО `version` → 0.0.17.

## ТЕСТ (пользователь)

1. Reload до 0.0.17 → открыть страницу с сохранёнными → в консоли страницы:
   `[CTX v07d] marked in scan: M; wrappers in DOM: N` — при исправной маркировке
   `N == M` (или близко, если часть якорей вне зоны).
2. ПКМ «Сохранить персонажа» → затем строка
   `[CTX v07d] anchors for <id>: K; wrapped: W` — в норме `W == K`
   (все якоря этого id обёрнуты).
3. При прокрутке/подгрузке ленты — `[CTX v07d] observer: scan scheduled`.

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS)

Логи самопроверки присутствуют и согласованы (`N≈M`, `W==K`); поведение
маркировки не изменилось (v07f6).

Б5: статус DONE — только после хэша коммита Пользователя.

СТОП.
