# TASK-0011 — fix(ext): v07f3 — удаление, чистый url, прозрачность, точка встречи=коммент

> Вердикт: v07f2 = FAIL по D13 → микро-фикс v07f3. Четыре правки, остальное НЕ трогать.

**КОММИТ:** `fix(ext): v07f3 — удаление, чистый url, прозрачность, точка встречи=коммент [TASK-0011]`
**СТАТУС:** NEW
**ХЭШ:** <заполняет Пользователь после пуша>
**ОБЛАСТЬ:** `ext` — EdgeExtension · **МАНИФЕСТ:** 0.0.13 · **CTX_BUILD:** "v07f3"
**ОСНОВАНИЕ:** v07f2 = FAIL по D13
**ЗАВИСИМОСТЬ:** v07f2

Остальное НЕ трогать (одно окно, «Удалить карточку», NAME_HINT, живой onChanged,
идемпотентность, контейнеры ≤3).

## ПРАВКИ

1. **УДАЛЕНИЕ ДОЛЖНО УДАЛЯТЬ:** в `dialog.js` по кнопке «Удалить карточку»:
   `db.cards = db.cards.filter(c => c.cardId !== cardId); CTX_STORAGE.saveDb(db).then(close)`.
   Контент по `storage.onChanged` сам снимет метки (unwrap+scan уже есть).
   Проверить, что кнопка привязана к обработчику.

2. **cleanUrl ВО ВСЕХ сохраняемых строках:** `identity.url`, `identity.metUrl` и
   `history.url` писать через `cleanUrl(link)` (trackcode/recom отсечены;
   reply/thread/w сохранены). Старые карточки не мигрировать — чистятся только
   новые записи.

3. **ЗАЛИВКА ПРОЗРАЧНЕЕ:** `.ctx-hl` background = цвет карточки с альфой ~25%
   (`hex + "40"`), текст ника читаем; dirt — grayscale+opacity по-прежнему.

4. **ТОЧКА ВСТРЕЧИ = ПЕРВОЕ ОБЩЕНИЕ:** контент при CAPTURED ищет якорь по href;
   если якорь внутри комментария (closest `wall_comments_comment_root` /
   `..._in_thread` / `li`), берёт внутри него `a[data-testid="wall_comment_date"]`
   и шлёт `commentUrl` (wall…?reply=…); SW пишет в `history.url` и
   `identity.metUrl = commentUrl`. Вне комментария — как сейчас (metPost).

## manifest.json

ТОЛЬКО version → 0.0.13.

## ТЕСТ (пользователь)

1. «Удалить карточку» реально удаляет (карточка исчезает из базы, метки снимаются).
2. Новые `identity.url` / `history.url` без trackcode/recom (reply/thread/w на месте).
3. Заливка ника прозрачная (~25%), текст читаем; dirt — блеклый.
4. ПКМ по имени в комментарии → точка встречи = `wall…?reply=…` (коммент), не metPost.
5. Одно окно, NAME_HINT, живой onChanged — без регрессий.

## ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS)

Пункты 1–5.

Б5: статус DONE — только после хэша коммита Пользователя.

СТОП.
