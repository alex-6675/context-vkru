dce4ca7 принят; v06r — рекомендую PASS с примечаниями

Доказательства в логах и скринах полные: ▲ с тултипами CTX: c14/c20/c11, marked 66 → 46 anchors после SPA-перехода, фикс D1 работает (разные reply одного поста → разные карточки c23/c24/c25), дедуп по дублю — «уже в базе (card c17)». Ваш вердикт зафиксируйте как PASS в RESULT_v06r.md с примечаниями R1–R3 ниже.

Ваши три наблюдения — формализую как требования

| № | Наблюдение (вашими словами) | Решение и этап |
|---|---|---|
| R1 | «Вася» в базе по id не опознаётся там, где VK грузит буквенную ссылку (скрин: Anna Andreeva без метки, а annaandreeva999 у поля ввода — с меткой) | v08r: (а) каноникализация при изъятии через редирект (fetch follow → response.url: /idNNN → короткий ник, если есть); хранить canonical + aliases; (б) ручное слияние ПКМ по немеченой ссылке: «прикрепить к карточке cN» |
| R2 | Нажатие на время пишет того, кого только что записали именем (время внутри чужой ссылки) | v08r: пункт «Сохранить комментарий» берёт пермалинк из DOM (ближайший wallcomment_date от цели ПКМ, которую контент-скрипт запомнил по событию contextmenu), а не linkUrl под курсором |
| R3 | «Сигналить заливкой ника цветом с картотеки (прозрачность 80%)» | v_07r: кроме ▲ — заливка ника цветом карточки (~80%); «грязь» — блеклость |

Про «лечить коррекцией AGENTS.md» — согласен, и вот почему это верно

Вы точно уловили: одной инструкцией нельзя управлять разными сущностями. У нас теперь три регламента для трёх ролей, и это правильно: INSTRUCTION.md — вы (человек), AGENTS.md — агент-исполнитель, регламент+архитектура — я (проектировщик). Поведение веб-Кодера при обрывах — это его операционная дисциплина, место ей в AGENTS.md. Внесите Дополнение Г:
```
ДОПОЛНЕНИЕ Г — ДИСЦИПЛИНА ОТВЕТА (исполняющий агент)
Г1. Один ответ = минимум один готовый артефакт (код-блок файла,
    отчёт, статус). Запрещено завершать ответ намерением
    («приступаю…») без артефакта.
Г2. Большую задачу агент в первом ответе объявляет списком шагов;
    каждый следующий ответ = один шаг с артефактом и маркером
    «ШАГ N ГОТОВ; далее: N+1».
Г3. «Продолжи» = продолжить с последнего артефакта; не перечитывать
    задачу с нуля, не повторять уже выданный код.
Г4. После обрыва связи: назвать последний завершённый шаг и запросить
    подтверждение продолжать; не начинать задачу заново.
Г5. Файлы — только код-блоками, путь в заголовке; один блок = один файл.
```
Коммит: docs: AGENTS.md — Дополнение Г (дисциплина ответа).

TASK-0011 — v_07r (manifest 0.0.10): карточка коррекции + цвет ника
```
МОДУЛЬ: M-07r · СБОРКА: v07r
КОММИТ: feat(ext): v07r — меню коррекции карточки, цвет ника, блеклость [TASK-0011]
ЦЕЛЬ: клик по метке открывает карточку коррекции; маркировка рисует
▲ + заливку ника цветом карточки (~80%); «грязь» — блеклость;
перерисовка без перезагрузки при изменении базы.

ФАЙЛЫ:
EdgeExtension/dialog.html + src/dialog.js (НОВЫЕ): окно карточки
  (chrome.windows.create из SW, url dialog.html#cardId);
  поля: displayName, note, status (saved/dirt/partner/vip),
  цвет (палитра 5), identities — список только для чтения;
  «Сохранить» → saveDb → закрыть. Скрипты: storage.js + dialog.js.
src/background.js: приём OPEN_CARD от контента → windows.create.
src/content.js: клик по span.ctx-mark → OPEN_CARD; рендер:
  обёртка span.ctx-hl вокруг якоря (background=цвет карточки,
  opacity .8) + ▲; status dirt → класс ctx-faded (opacity .45 +
  grayscale); chrome.storage.onChanged → пересобрать индекс и
  перерендерить (снять свои обёртки, применить заново).
src/styles.css: .ctx-hl, .ctx-faded, палитра; только классы ctx-*.
manifest: version 0.0.10; новых permissions НЕ нужно.

ШАГИ ПО ДОПОЛНЕНИЮ Г (один ответ = один шаг):
 1) dialog.html + dialog.js; 2) content.js рендер + onChanged;
 3) styles.css + manifest + отчёт.

ТЕСТ: 1) клик по ▲ → окно карточки; 2) статус «грязь» → ник блеклый
без F5; 3) цвет → заливка ника ~80%; 4) примечание и статус живут
после F5; 5) клики VK не нарушены.
PASS: пункты 1–5. R1/R2 в этот этап НЕ входят (пойдут в TASK-0012/v_08r).
```
Порядок: вы фиксируете PASS по v06r + коммитите Дополнение Г → Кодер берёт TASK-0011 по шагам Г1–Г5 → хэш мне → ваш тест → затем TASK-0012 (v_08r: R1+R2 — каноникализация, слияние, честный «сохранить комментарий»).

СТОП.
