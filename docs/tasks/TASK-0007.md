# TASK-0007 — fix(dashboard): завершение ремонта, data-модули, статусы

**КОММИТ:** `fix(dashboard): завершение ремонта, data-модули, статусы [TASK-0007]`
**СТАТУС:** DONE
**ХЭШИ:** часть 1 — `4c307da` (гигиена tasks.ts / audit.ts / TaskRegistry.tsx);
завершающий коммит — <заполняет Пользователь после пуша>
**ОБЛАСТЬ:** `dashboard` — журнал процесса
**ОСНОВАНИЕ:** сверка проектировщика по коммиту 4c307da (ШАГ 1)

## ЦЕЛЬ

Завершить ремонт дашборда после системной порчи при передаче
(«дописывание второй копии» + потерянные файлы): восстановить недостающие
модули, привести статусы и блок порядка к фактическому списку задач.

## ИЗМЕНЯЕМЫЕ ФАЙЛЫ

- `dashboard/src/data/v03f.ts` — восстановлен (одна копия, дословно из источника)
- `dashboard/src/data/v03f2.ts` — восстановлен (одна копия)
- `dashboard/src/data/v03r.ts` — восстановлен (одна копия)
- `dashboard/src/components/V03RSection.tsx` — восстановлен (одна копия)
- `dashboard/src/components/V03F2Section.tsx` — восстановлен (одна копия)
- `dashboard/src/data/tasks.ts` — статус TASK-0003 = done-pass (a4b6ec9);
  добавлены записи TASK-0006 (v04r) и TASK-0007
- `dashboard/src/components/TaskRegistry.tsx` — локальная правка: блок
  «Порядок исполнения группы» в соответствие со списком задач
  (TASK-0006 = v04r; номера v05r/v06r/v07r будут выданы позже)

## ПРИЁМКА (по Б3)

- панель Problems по dashboard/src = 0 ошибок;
- `npm run build` — проходит;
- функциональность и данные журнала (шаги, статусы) сохранены;
- `Select-String '修改后'` по dashboard/src и EdgeExtension → 0.

## РЕЗУЛЬТАТ

Сборка зелёная, все импорты резолвятся, одна копия на файл.
Часть 1 (4c307da) принята сверкой проектировщика; завершение — настоящим коммитом.

СТОП.
