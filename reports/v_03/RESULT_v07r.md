# RESULT · v07r · Меню коррекции карточки, цвет ника, блеклость

**СТАТУС:** FAIL
**Проверил:** Пользователь
**Сборка:** v07r (manifest 0.0.10)

## Дефекты (D1–D4)

- **D1 — неточная маркировка.** Несколько ▲ на одну карточку в одном контейнере,
  метились якоря без текста (аватары), дубли меток при перерисовке.
- **D2 — лишний ◆ и мусорные wall-карточки.** Закладки ◆ создавались без нужды;
  ПКМ по дате комментария плодил wall-карточки вместо того, чтобы извлечь автора.
- **D3 — нет удаления карточки.** Карточку нельзя было удалить из диалога.
- **D4 — несколько окон на одну карточку.** Повторный клик по метке открывал
  новое окно вместо фокуса на уже открытом.

## Решение №5 (фикс-итерация v07f)

Точечная маркировка (только ▲, один на карточку на контейнер, якоря без текста
не метить); изъятие автора из комментария по ПКМ на дате (без ◆, без мусорных
wall-карточек); удаление карточки из диалога; одно окно на карточку
(карта cardId→windowId); живой рендер без F5 (storage.onChanged → unwrap + scan)
и NAME_HINT (имя из первого якоря).

## Вердикт

v07r = FAIL. Фикс-итерация: **v07f** (manifest 0.0.11, CTX_BUILD "v07f").
