Вердикт по v_03: FAIL (частичный) — причина найдена, она системная

Доказательная база из трёх файлов

| Селектор в моём коде | Тип разделителя | Работает в проде? |Evidence |
|---|---|---|---|
| [data-testid="post"], post-header, post-header-title | дефис | ✅ ДА | POST-сущности найдены на всех страницах, авторы извлечены |
| [data-testid="postdateblock_preview"] | подчёркивание | ❌ НЕТ | Во всех логах post=- (postUrl не извлёкся) |
| a[data-testid="wallcommentdate"], wallcommentscomment_root… | подчёркивание | ❌ НЕТ | 0 COMMENT при доказанном наличии date-ссылок (дамп ссылок /kursporezbe: ?reply=1014 и др.) |

Корень причины: ваш DIAG-пейст в консоли содержит wallcommentdate / wallcommentscomment_root — подчёркивания съедены markdown-рендером при копировании из чата. Тот же дефект в локальном vkru.js: селекторы с дефисами работают, с подчёркиваниями — нет. Это дефект передачи кода, а не архитектуры.

Дополнительно: в репо (ветка main-qwenv02) файл adapters/vkru.js — всё ещё пустой КАРКАС. Код v_03 в репо не закоммичен, что лишило нас возможности верификации. Это нарушение процесса.

Процессуальные решения (фиксация в РЕГЛАМЕНТ/AGENTS)

Код передаётся только закоммиченным файлом. После каждой итерации Coder коммитит изменённые файлы EdgeExtension/ в ветку; проектировщик верифицирует raw-файл на GitHub до теста пользователем.
Запрет копирования кода из рендер-вью чата (markdown съедает _). Копировать только из raw/исходника сообщения или из файла в VS Code.
Самодиагностика вместо внешних snippet'ов: content.js сам логирует покрытие селекторов (coverage posts/dates/roots) — порча селекторов видна мгновенно, без DIAG-пейстов.

МОДУЛЬ: M-03-fix (внутри Этапа 2, работа после FAIL по §21)
СБОРКА: v_03f (manifest 0.0.4)

ЦЕЛЬ:
Восстановить comment-ветку обнаружения (исправленные селекторы),
добавить самопроверку покрытия, ЗАКОММИТИТЬ код в репо для верификации.

ИЗМЕНЯЕМЫЕ ФАЙЛЫ:
EdgeExtension/src/adapters/vkru.js   (полная реализация, raw-источник!)
EdgeExtension/src/content.js         (+ coverage-лог)
EdgeExtension/src/core/messaging.js  (CTXBUILD="v03f")
EdgeExtension/manifest.json          (version 0.0.4)

ЧТО СДЕЛАНО:
vkru.js — код Этапа 2 без изменений логики; взят СТРОГО из raw-источника
   задания (не из рендер-вью!). Селекторы с подчёркиваниями:
   wallcommentdate, wallcommentscomment_root,
   wallcommentscommentinthread, postdateblock_preview, comment-owner,
   comment-text — должны присутствовать в файле дословно.
content.js — coverage-лог при каждом изменении счётчиков:
   «[CTX v_03f] coverage posts/dates/roots = N/M/K». M>0 на странице
   с комментариями = селекторы живы.
РЕПО-ГЕЙТ: после сохранения Coder коммитит все изменённые файлы
   в main-qwenv02 и возвращает ссылку на коммит. Пользователь НЕ тестирует
   до моей верификации raw-файлов.

КАК СОБРАТЬ / ЗАГРУЗИТЬ:
Сохранить UTF-8 → Reload в edge://extensions → версия 0.0.4 → F5 на vk.ru.

ТЕСТ (после верификации проектировщиком):
P1 /collabis (или /kursporezbe) с раскрытыми комментариями:
  1) coverage: dates>0 и roots>0;
  2) строки COMMENT с post=wall-228585143_… и reply=…;
  3) SW: entities reported включает COMMENT.
P2 feed?w=wall-234325978_1730: POST (post=wall-…) + COMMENT.
P3 /id815989371: POST PERSON + комментарии.
P4 example.com: тишина.

ОЖИДАЕМЫЙ РЕЗУЛЬТАТ (PASS):
coverage dates>0 на P1–P3;
COMMENT обнаружены (включая треды), postUrl извлечён (post≠-);
дедуп без повторов; SW получил COMMENT; P4 тишина; карточка без ошибок.
Результат → reports/v03/RESULTv_03.md (статус по fix) и dashboard.

СТОП.

Порядок действий сейчас: Coder реализует и коммитит → шлёте мне ссылку на коммит → я читаю raw vkru.js/content.js и подтверждаю целостность селекторов → только затем пользователь тестирует P1–P4.

СТОП.