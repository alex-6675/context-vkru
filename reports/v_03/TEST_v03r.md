# TEST · v03r · M-03r (по M03r.md)

> **РЕПО-ГЕЙТ:** пользователь НЕ тестирует до коммита в main-qwen_v_03
> и raw-верификации файлов проектировщиком.

## 1. Предусловия

- Reload (⟳) в edge://extensions → версия **0.0.6**, без Errors.
- Открыта вкладка `https://vk.ru/` со стеной (любой пост с именем автора-ссылкой).
- Консоль F12 с фильтром `CTX` (контекст: страница).

## 2. Три действия (скриншот)

1. **ПКМ на имени автора любого поста** → в контекстном меню видны пункты
   «Сохранить персонажа» и «Сохранить сообщество».
2. Навести курсор на имя (внизу слева появится ссылка) и кликнуть
   **«Сохранить персонажа»** → в консоли строка:
   `[CTX v03r] captured | menu: save-person | link: … | page: …`,
   и **link совпадает** с тем, что было в статус-строке внизу слева.
3. **ПКМ по пустому месту страницы** → наших пунктов **НЕТ**.

## 3. Ожидаемый результат (PASS)

- пункты есть **только на ссылках**;
- изъятая `link` совпадает со статус-строкой;
- `page` = текущая страница;
- карточка расширения без ошибок.

→ `RESULT_v03r.md` и dashboard, затем **v_04r** (опознание портала +
нормализация id + авто-тип).

## 4. Признаки FAIL

- пунктов нет на ссылке (проверить Reload и версию 0.0.6; `permissions`);
- пункты видны при ПКМ по пустому месту (`contexts` повреждён);
- `link` пуст или не совпадает со статус-строкой;
- красные ошибки в SW-консоли.

## 5. RESULT

Фиксация — в `reports/v_03/RESULT_v03r.md` и в dashboard.
Вердикт устанавливает только Пользователь (§5.6).
