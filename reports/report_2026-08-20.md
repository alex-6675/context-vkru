# Отчёт · сессия 2026-08-20 · M-03-fix2 / v03f2 (порче-невозможная реализация)

**МОДУЛЬ:** M-03-fix2 (внутри Этапа 2, после FAIL v_03f) · **СБОРКА:** v03f2 · **МАНИФЕСТ:** 0.0.5
**ВЕТКА:** main-qwen_v_03

---

## Вердикт 3C (принят)

Прод повреждён, архитектура здорова. Замер на /feed: ground truth `dates=168`,
`roots+inthread=97+71=168`; coverage расширения — `dates=168 / roots=0`.
Roots-селекторы съедены порчей передачи кода (подчёркивания). Архитектурное
решение 3A (closest по testid) подтверждено полностью, fallback для /feed не нужен.

## Что сделано в сессии

Дамп v03f2 разнесён в структуру репозитория (ветка `main-qwen_v_03`):

**Пакет расширения (EdgeExtension/):**
- `manifest.json` — только `version: 0.0.5`;
- `src/core/messaging.js` — `CTX_BUILD = "v03f2"`; имена констант и типы сообщений
  без изменений (`CTX_BUILD` / `CTX_MSG.ENTITY_FOUND`) — совместимость с background.js;
- `src/adapters/vkru.js` — полная замена: все testid/regex с `_` собираются через
  `U = String.fromCharCode(95)` (в исходнике литеральных подчёркиваний нет, §25);
  POST: postUrl из атрибута `data-post-id` (CONFIRMED на 100%), без postdate-селектора;
  COMMENT: date → closest(T_ROOT) → owner/text/reply/thread (как в 3A);
- `src/content.js` — полная замена: coverage-гейт `posts/dates/roots`
  (логируется при изменении), селекторы coverage собраны через U;
- `src/background.js` — **НЕ меняется** (приём ENTITY_FOUND и PING/PONG как в v_03).

**Отчёты (reports/v_03/):**
- `BUILD_v03f2.md` — состав сборки + вердикт 3C + правило порче-невозможности (§25);
- `TEST_v03f2.md` — минимальный тест на /feed с РЕПО-ГЕЙТом и приёмочным гейтом `roots>0`;
- `report_v_03_f_fix.md` — задание разработчика (уже было в ветке, принято дословно).

**Dashboard:** не пересобирался (договорённость — пересборкой дашборда не занимаюсь).

## Регламент запуска (после raw-верификации проектировщиком)

1. `edge://extensions` → Reload (⟳) → версия **0.0.5**, без Errors.
2. Открыть страницу **/feed**, две консоли (страница F12 с фильтром `CTX` + Service Worker).
3. **ГЕЙТ:** `[CTX v03f2] coverage posts/dates/roots = 4/…/…` с `roots>0`
   (ожидание ≈ `4/168/168`). `roots=0` → код снова повреждён: СТОП и сообщить.
4. Строки `COMMENT` с `post=wall-…` и `reply=…`; `POST` теперь с `post=wall-…` (не «-»).
5. SW-консоль: `entities reported` включает COMMENT.
6. `/collabis` — опционально, те же критерии.

## Приёмочный гейт и процесс

- Порче-невозможность (§25): в строках-селекторах и regex **нет литеральных
  подчёркиваний** — они собираются в runtime через `String.fromCharCode(95)`;
- `coverage roots>0` — признак того, что код дошёл до браузера живым;
- РЕПО-ГЕЙТ: пользователь НЕ тестирует до коммита и raw-верификации файлов.

## Следующий этап

- Фиксация вердикта — `reports/v_03/RESULT_v_03.md` (статус по fix2) — устанавливает
  только Пользователь (§5.6).
- После PASS проектировщик закрывает v_03 и выдаёт **M-04 (Этап 3 — Navigation Target)**
  отдельным заданием.

---

*Сформировано SourceCraft. PASS/FAIL по v_03 (fix2) устанавливает исключительно
Пользователь после ручной проверки в Edge (§5.6).*