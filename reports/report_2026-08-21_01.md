# Отчёт · v06r (TASK-0010) · 2026-08-21_01

Версия отчёта: **v_01**
Статус: **NOT TESTED** (ожидает ручной проверки Пользователем в Edge, §5.6)

## TASK

Разнос эталонного кода **v06r** (TASK-0010, manifest **0.0.9** — маркировка ▲/◆
после F5 + фикс дедупа D1) из дампа `task-0010.log` в структуру репозитория,
создать отчёт и закоммитить.

## CHANGED FILES

- `EdgeExtension/manifest.json` — версия 0.0.8 → **0.0.9**; `permissions` уже
  содержат `contextMenus` и `storage`; в content_scripts добавлены
  `src/core/normalize.js` и `css: ["src/styles.css"]`.
- `EdgeExtension/src/core/messaging.js` — v06r (`CTX_BUILD="v06r"`,
  `CTX_MSG.CAPTURED`). — ранее.
- `EdgeExtension/src/core/normalize.js` — v06r (`portalOf/normalize/metPostOf/
  replyOf` → `CTX_NORMALIZE`; regex с `_` через `U = String.fromCharCode(95)`). — ранее.
- `EdgeExtension/src/core/storage.js` — **без изменений** (как v05r).
- `EdgeExtension/src/background.js` — v06r (фикс D1: дедуп COMMENT по
  `portal+id+replyId`, `replyId` в identity). — ранее.
- `EdgeExtension/src/content.js` — v06r (маркировка ▲ персонаж/сообщество,
  ◆ закладка комментария; только `span.ctx-mark`; WeakSet; MutationObserver 600 мс). — ранее.
- `EdgeExtension/src/styles.css` — **НОВЫЙ** (`.ctx-mark`, `.ctx-mark-c`). — ранее.

## CHANGES

- D1: ключ дедупа COMMENT = `portal + id + replyId` (ранее portal+id схлопывал
  разные комментарии поста в одну карточку).
- Маркировка сохранённых ссылок после F5: ▲ (персонаж/сообщество),
  ◆ (закладка комментария). Меняется только добавленный `span.ctx-mark`,
  атрибуты VK не трогаются (§H).
- `replyId` извлекается через `replyOf()` и сохраняется в `identity.replyId`.

## BUILD

Команда: (Build.ps1 без zip — по reference-репо serv6675/context)
Результат: **NOT TESTED** — сборка выполняется локально Пользователем/разработчиком
после raw-верификации (РЕПО-ГЕЙТ).

## TEST

Ручной тест Пользователя в Edge (после Reload, версия 0.0.9):
- метки ▲/◆ появляются на сохранённых ссылках после F5;
- D1: два разных комментария одного поста = две карточки (не схлопываются);
- вёрстка VK не повреждена (меняется только добавленный span).

## RESULT

**NOT TESTED**

## NOTES

- PASS по v06r — только Пользователь после ручной проверки (§5.6);
  raw-верификация кода разработчиком до теста (РЕПО-ГЕЙТ).
- Порче-невозможность §25 соблюдена: литеральных `_` в коде нет,
  селекторы собираются через `String.fromCharCode(95)`.
- D2 (коррекция типа при повторном изъятии того же id) — отложен до v_07r.
- Git (origin2/GitHub) недоступен — верификация идёт через чат.